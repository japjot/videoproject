<%= form_for(@invite) do |f| %>
  <% if @invite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invite.errors.count, "error") %> prohibited this invite from being saved:</h2>

      <ul>
      <% @invite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.hidden_field :first_name, :value => @friend.first_name  %>
    <%= f.hidden_field :last_name, :value => @friend.last_name %>
    <%= f.hidden_field :linkedin_id, :value => params[:linkedin_id] %>

  <div class="field">
    <%= f.label :subject %><br />
    <%= f.text_field :subject, :value => @invite_subject %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body, :value => @invite_body  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
