<% @comments.each do |comment| %> 
    <h3> <%= comment.title %> </h3>
    <p> <%= comment.comment %> </p>
<% end %> 

<%= form_tag :action => 'comment' do  %>
	Comment: <br />
	<%= text_area 'comment', 'comment', :rows => 10, :class => 'input-xlarge' %><br />

	<%= submit_tag 'Add Comment' %>
	<%= hidden_field 'commentable', 'commentable_id', :value => @video.id %>
<% end  %>
